#include <stdio.h>

/**
 * main - entry point
 *
 * Return: return 0 always (success)
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%i", a);
	}
	putchar('\n');

	return (0);
}
