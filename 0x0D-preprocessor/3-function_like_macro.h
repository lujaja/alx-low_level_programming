/**
 * ABS(x) - macro name
 * Description - this header file creates a function like macro to calculate absolute value of any given number.
 */

#ifndef FUNCTION_LIKE_MACRO
#define FUNCTION_LIKE_MACRO

#define ABS(x) ((x) < (0) ? (-x) : (x))

#endif
