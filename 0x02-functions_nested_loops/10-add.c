#include "main.h"

/**
 * add - adds two integers and return results
 * Return: results
 * @a: number
 * @b: number
 */

int add(int a, int b)
{
	int results;
	
	results = a + b;
	return (results);
}
